import { PROVIDER_LATENCY_EVENT_NAME } from '@slatrap/slatrap';

export const PLAID_ITEM_CREATED = 'plaid.item.created';

export const PROVIDER_ERROR_EVENT = 'provider.error';

/** Re-export of SDK canonical name so packages stay in sync. */
export const PROVIDER_LATENCY_EVENT = PROVIDER_LATENCY_EVENT_NAME;
