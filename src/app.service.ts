import { Injectable } from '@nestjs/common';

/**
 * Root service for the application.
 * Currently a placeholder - extend as application-level concerns emerge.
 */
@Injectable()
export class AppService {}
